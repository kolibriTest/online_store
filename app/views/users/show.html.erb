<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Профіль користувача</title>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Підключення Bootstrap для стилів -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <!-- Підключення AOS для анімацій -->
  <link href="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.css" rel="stylesheet">
  
  <!-- Підключення FontAwesome для іконок -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
  
  <!-- Стилі -->
  <style>
    .profile-header {
      text-align: center;
      margin-bottom: 40px;
      font-family: 'Arial', sans-serif;
      font-weight: 600;
    }

    .profile-info p {
      font-size: 1.1rem;
      line-height: 1.6;
    }

    .profile-info strong {
      font-weight: bold;
    }

    .btn-custom {
      border-radius: 20px;
      padding: 10px 30px;
      font-size: 1rem;
    }

    .btn-warning {
      background-color: #f0ad4e;
      border-color: #f0ad4e;
    }

    .btn-warning:hover {
      background-color: #ec971f;
      border-color: #d58512;
    }

    .btn-secondary {
      background-color: #6c757d;
      border-color: #6c757d;
    }

    .btn-secondary:hover {
      background-color: #5a6268;
      border-color: #545b62;
    }

    /* Адаптація для мобільних */
    @media (max-width: 768px) {
      .profile-info p {
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>
  <div class="container mt-5" data-aos="fade-up" data-aos-duration="1000">
    <div class="profile-header">
      <h1>Профіль користувача</h1>
    </div>

    <div class="profile-info">
      <p><strong>Ім'я:</strong> <%= @user.name %></p>
      <p><strong>Email:</strong> <%= @user.email %></p>
      <p><strong>Дата реєстрації:</strong> <%= @user.created_at.strftime("%Y-%m-%d") %></p>
    </div>

    <div class="mt-4 text-center">
      <%= link_to 'Редагувати профіль', edit_user_path(@user), class: "btn btn-warning btn-custom" %>
      <%= link_to 'Назад', root_path, class: "btn btn-secondary btn-custom" %>
    </div>
  </div>

  <!-- Підключення AOS і Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js"></script>
  <script>
    AOS.init(); // Ініціалізація анімацій AOS
  </script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
